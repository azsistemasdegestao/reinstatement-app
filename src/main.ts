import { bootstrapApplication } from '@angular/platform-browser';
import { ReinstatementGuideComponent } from './app/reinstatement-guide/reinstatement-guide.component';

bootstrapApplication(ReinstatementGuideComponent)
  .catch(err => console.error(err));

